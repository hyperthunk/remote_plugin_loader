
{post_hooks, [
    {clean, "rm build/plugins/remote_plugin_loader.erl"}
]}.

{plugin_dir, "build/plugins"}.
{plugins, [remote_plugin_loader,
           rebar_skip_deps]}.

{plugin_repositories, [
   % {github, "hyperthunk"},
   {bitbucket, "hyperthunk"}
]}.
